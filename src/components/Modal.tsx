'use client'

export default function Modal({ closeModal }: { closeModal: () => void }) {
	return (
		<div>
			<h2 className='font-semibold text-[26px]'>
				Welcom to <span>Doubtss.com</span>
			</h2>
		</div>
	)
}
